
/*
const localStorageContent = localStorage.getItem('users');

let users;

if(localStorageContent === null) {
    users = [];
} else {
    users = JSON.parse( localStorageContent );
}

var obj = {
    name: 'Nick',
    age: 44
}

users.push(obj);


localStorage.setItem('users', JSON.stringify( users ) );
*/

/*localStorage.setItem('users', person);*/

